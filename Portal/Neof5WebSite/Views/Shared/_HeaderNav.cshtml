<nav class="navbar navbar-default navbar-inverse navbar-fixed-top" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <a class="navbar-brand" href="~/">ネオ富豪</a>
  </div>

  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    <ul class="nav navbar-nav">
      <li class="@(Request.Url.AbsolutePath == "/" ? "active" : "")">
		  <a href="~/">トップ</a></li>
      <li class="@(Request.Url.AbsolutePath.StartsWith("/rule") ? "active" : "")">
		  <a href="~/rule/">ルール</a></li>
      <li class="dropdown">
        <a href="~/waitingroom/" class="dropdown-toggle" data-toggle="dropdown">プレイ<b class="caret"></b></a>
        <ul class="dropdown-menu">
          <li><a href="/waitingroom/quickplay">クイックプレイ</a></li>
          <li><a href="http://@Constants.MASTER_SITE_HOSTPORT()/playroom/" target="_blank">プレイルーム</a></li>
        </ul>
      </li>
      <li class="@(Request.Url.AbsolutePath.StartsWith("/spec") ? "active" : "")">
		  <a href="~/spec/">プログラム仕様</a>
      </li>
      <li class="@(Request.Url.AbsolutePath.StartsWith("/test") ? "active" : "")">
        <a href="~/test/">テスト</a>
      </li>
@*      <li class="@(Request.Url.AbsolutePath.StartsWith("/entry") ? "active" : "")">
		  <a href="~/entry/">エントリー</a>
      </li> *@
      <li class="@(Request.Url.AbsolutePath.StartsWith("/results") ? "active" : "")">
        <a href="~/results/">大会結果</a>
      </li>
    </ul>
  </div><!-- /.navbar-collapse -->
</nav>
